def f(int x, int y, int z) : bool {
  int a = x + y
  return a or z
}

def one(int x) : int{
  return 1
}

def main() : int{
  bool tmp = f(8 * 2, 4, 1)
  if(tmp) {
    print(4242)
  }
  int o = one(3)
  print(o)
  return 0
}
